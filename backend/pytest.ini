[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests (isolated, mocked dependencies)
    integration: Integration tests (real API endpoints, test DB)
    slow: Slow tests (API calls, database)
